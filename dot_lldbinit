
settings set frame-format "frame #${frame.index}:{ ${frame.no-debug}${frame.pc}}{ ${module.file.basename}{`${function.name-with-args}{${frame.no-debug}${function.pc-offset}}}}{ at ${line.file.fullpath}:${line.number}}{${function.is-optimized} [opt]}\n"

script
import os
if "llvm-project" in os.getcwd():
    lldb.debugger.HandleCommand("command script import llvm/utils/lldbDataFormatters.py")
